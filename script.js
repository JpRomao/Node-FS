const fs = require('fs');

/*
  fs.writeFile('teste.txt', 'Hello World\r\n', error => {
    if(error) {
      throw error;
    }

    console.log("Arquivo criado com sucesso");
  });
*/

/*
  fs.appendFile('teste.txt', 'Hello World\r\n', error => {
    if(error) {
      throw error;
    }

    console.log("Adicionado ao arquivo com sucesso");
  });
*/

/*
  fs.unlink('teste.txt', error => {
    if(error) {
      throw error;
    }

    console.log("Arquivo deletado com sucesso!");
  });
*/

/*
fs.readFile('teste.txt', 'UTF8', (error, data) => {
  if(error) {
    throw error;
  }

  console.log(data);
});
*/

/*
  fs.rename('teste.txt', 'novoTeste.txt', error => {
    if(error) {
      throw error;
    }

    console.log("Arquivo renomeado com sucesso!");
  });
*/